AUTOMAKE_OPTIONS = foreign

# this lists the binaries to produce, the (non-PHONY, binary) targets in
# the previous manual Makefile
bin_PROGRAMS = ReadTest

# c++ flags
ReadTest_CFLAGS = -g -O2 -Wall
ReadTest_SOURCES = ReadTest.c 
